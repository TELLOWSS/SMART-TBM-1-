# 안전데이터 심층연구소 v2 구현 계획서

## 목표
- 안전데이터 심층연구소를 **현장 지휘형 안전 운영체계**로 업그레이드
- 인사이트 제공에서 끝나지 않고, 실행지시/추적/검증까지 연결
- 체크리스트 기반으로 순차 진행 후 `Clear` 처리

## 아이덴티티 반영 원칙
- 지휘성: 분석 결과를 즉시 실행 가능한 지시로 전환
- 현장성: 팀/공종/작업 맥락 중심
- 증빙성: 근거 데이터/변경 이력/리포트 스냅샷 보존
- 선제성: 사고 후 대응이 아닌 조기 경보 중심

## 단계별 구현 (순차 진행)

### Phase 1 — 필터/탐색 고도화 (P0)
- [x] 기간 필터 추가 (7일/30일/월간/커스텀)
- [x] 다중 필터 추가 (팀, 위험, 리더, 점수구간)
- [x] 필터 상태 뱃지/초기화 UX 통일
- [x] 필터 적용 상태 접근성 문구 정리

**Clear 기준**
- [x] 필터 조합 10개 케이스 정상 동작
- [x] 빈 데이터/대용량 데이터 모두 렌더링 오류 없음
- [x] `npm run build` 통과

✅ **Phase 1 Cleared — 2026-04-28**

**Phase 1 검증 케이스 (10)**
1. [x] 기간 `7D` 단독 적용
2. [x] 기간 `30D` 단독 적용
3. [x] 기간 `THIS_MONTH` 단독 적용
4. [x] 기간 `CUSTOM` + 유효 범위(start<=end)
5. [x] 기간 `CUSTOM` + 무효 범위(start>end) 처리
6. [x] 팀 필터 단독 적용
7. [x] 위험 필터 단독 적용
8. [x] 팀 + 위험 동시 적용
9. [x] 팀 + 위험 + 기간 동시 적용
10. [x] 필터 초기화 시 기본값(`30D`) 복귀

---

### Phase 2 — 분석 정확도 강화 (P0)
- [x] 위험 키워드 사전 구조화 (하드코딩 분리)
- [x] 관리 가능한 위험 분류 체계 도입
- [x] 트렌드 차트 동적 스케일로 수정
- [x] 상위 위험요인 계산 로직 검증

**Clear 기준**
- [x] 샘플 데이터 3세트에서 스펙트럼 결과 검증
- [x] 트렌드 왜곡 없음 (저/중/고 데이터)
- [x] `npm run build` 통과

✅ **Phase 2 Cleared — 2026-04-28**

---

### Phase 3 — AI 지시 체계화 (P1)
- [x] AI 결과를 "지시 카드" 포맷으로 변환
- [x] 지시 항목: 담당팀/우선순위/기한/KPI
- [x] 근거 데이터 요약(왜 이 지시인지) 포함
- [x] 실패/재시도/로딩 상태 메시지 정교화

**Clear 기준**
- [x] 지시 카드 3개 이상 자동 생성
- [x] 필터 맥락 변경 시 결과 일관성 유지
- [x] `npm run build` 통과

✅ **Phase 3 Cleared — 2026-04-28**

---

### Phase 4 — 비교/추적/자산화 (P1)
- [x] 전주/전월 비교 지표 추가
- [x] 분석 스냅샷 저장 구조 추가
- [x] 내보내기(PDF/CSV) 최소 1종 도입
- [x] 결과 공유를 위한 텍스트 요약 템플릿 제공

**Clear 기준**
- [x] 비교 지표 5개 정상 계산
- [x] 스냅샷 저장/조회 동작 확인
- [x] `npm run build` 통과

✅ **Phase 4 Cleared — 2026-04-28**

---

## 확장 카테고리 — 스마트TBM지휘 (신규)

### 카테고리 목표
- TBM 기록을 넘어, 당일 작업의 **지휘·지시·이행확인** 루프를 완결
- 팀장/관리감독자/안전담당의 역할별 지시를 분리하고 실행 증빙까지 연결
- SafetyDataLab 분석 결과를 현장 지휘 액션으로 즉시 변환

### 카테고리 구성 (v1)
1. **지휘브리핑**: 당일 핵심위험 3건 + 우선 지시사항 자동 제시
2. **지시발령**: 담당팀/담당자/마감시각/확인방식 포함 지시 생성
3. **이행체크**: 지시별 완료/지연/미착수 상태 및 증빙(사진/코멘트) 추적
4. **지휘리포트**: 일일 지휘결과 요약(지시 수, 완료율, 지연 사유) 자동 정리

### 스마트TBM지휘 단계별 구현 (순차 진행)

#### Command Phase 1 — 지휘브리핑 보드 (P0)
- [x] 당일 위험도 상위 3개 자동 추출
- [x] 브리핑 카드(위험요인/영향/즉시조치) UI 추가
- [x] 팀별 브리핑 대상 필터(전체/팀별)

**Clear 기준**
- [x] 샘플 3세트에서 상위 위험 3건 일관 추출 (동일 로직 기준)
- [x] 빈 데이터에서 안전한 fallback 메시지 출력

✅ **Command Phase 1 Cleared — 2026-04-29**

#### Command Phase 2 — 지시발령 워크플로우 (P0)
- [x] 지시 발령 스키마 정의 (`지시ID/담당/기한/우선순위/근거`)
- [x] 수동+AI 추천 지시 혼합 생성
- [x] 지시 상태 기본값(`미착수`) 및 타임스탬프 저장

**진행 메모 (2026-04-29)**
- 수동 지시 생성/상태변경/삭제 + 로컬 저장(localStorage) 구현
- 모바일 터치 최소 높이(40~44px), 폼/목록 반응형 그리드 적용

**Clear 기준**
- [x] 지시 생성/수정/삭제 CRUD 정상 동작
- [x] 필터 전환 시 지시 데이터 무결성 유지

✅ **Command Phase 2 Cleared — 2026-04-29**

#### Command Phase 3 — 이행체크/증빙 (P1)
- [x] 상태 전이(`미착수→진행중→완료/지연`) UI
- [x] 완료 증빙(사진/텍스트) 첨부
- [x] 지연 사유 코드화(자재/인원/기상/기타)

**Clear 기준**
- [ ] 10건 이상 지시의 상태 변경 이력 보존
- [ ] 증빙 첨부/조회/삭제 동작 확인

**진행 메모 (2026-04-29)**
- 지시 카드별 증빙 이미지(최대 3장) 첨부/삭제, 코멘트 입력 구현
- 지연 사유 코드 및 상세 사유 입력 구현
- 상태 전이 시 `statusHistory` 누적 저장 및 카드 내 이력 조회 UI 구현
- 상태전이 누적 건수 자동 집계 및 `10건+` 기준 충족 여부 배지 표시 추가
- 검증 가속을 위한 `검증용 5건 생성`/`상태전이 자동 검증` 액션 추가
- 검증 종료 후 샘플 정리를 위한 `검증용 데이터 정리` 액션 추가
- 평가자/실무자 관점 검증 배지(이력·리포트 / 증빙·지연사유) 추가
- 등록 화면 모바일 하단 고정 저장바 및 입력진척 요약 추가

#### Command Phase 4 — 지휘리포트/회고 (P1)
- [x] 일일 지휘 리포트 요약 생성
- [x] 완료율/지연율/재발위험 지표 산출
- [x] 공유용 텍스트 템플릿 제공

**Clear 기준**
- [ ] 리포트 지표 5개 이상 정상 계산
- [ ] 공유 텍스트 생성 및 복사 동작 정상

**진행 메모 (2026-04-29)**
- Command Daily Report 초안 UI 추가(총지시/완료/지연/완료율/지연율)
- 지연 사유 Top, 주요 위험요인 Top3 요약 추가
- `지휘 리포트 복사` 액션 추가
- 재발위험 지표(지연 지시와 주요 위험요인 연관 비율) 추가
- 트래커 입력용 `검증 로그 1줄 복사` 액션 추가(완료/진행중 자동 판정)
- Phase3/4 검증 후 운영 데이터 정합성 유지를 위한 샘플 정리 루프 확보
- Phase3/4 통합 검증 상태 카드(증빙/지연사유/리포트데이터/완료조건) 추가
- Phase3/4 클리어 기준 자동 체크리스트 + `클리어 요약 1줄 복사` 액션 추가
- 스마트 TBM 등록 화면 모바일 레이아웃 최적화(고정 2분할 → 모바일 우선 스택)
- 스마트 TBM 등록 화면 모바일 탭 전환 적용(미디어/입력데이터), 입력란 전체폭 확보
- 스마트 TBM 지휘 화면 모바일 빠른 검증 액션 바(로그/클리어 요약 복사) 추가
- 문서 보관소 연계 여부 필터(`연계 전체/있음/미연계`) 추가
- 대시보드 금일 TBM 동일월 위험성평가 연계 경보 카드 추가
- 심층연구소 연계 사용률/동일월 연계율/평균 상위위험 KPI 추가
- 문서 보관소 `동일월 연계/동일월 미일치` 세부 필터 추가
- 대시보드 경보 카드에서 문제 TBM `연계 보정 바로가기` 제공
- 심층연구소 팀별 연계 Top 보드 추가
- 보관소 카드 `연계 보정 필요 / 동일월 확인 필요` 배지 추가
- 대시보드 금일 연계 이슈를 팀별 우선순위 묶음으로 표시
- 심층연구소 최근 6개월 연계율/동일월 연계율 추이 차트 추가
- 보관소 `보정 필요만` 빠른 토글 추가
- 심층연구소 추이 차트 범위 `3M / 6M / 12M` 전환 추가
- 대시보드 팀별 경보 카드 클릭 시 해당 팀 금일 TBM만 즉시 확인 가능하도록 연동
- 대시보드 상단 헤더에 정규화 워크플로우 경보(`CRITICAL/WARNING`) 배지 연동
- 대시보드에서 심층연구소로 즉시 점프 가능한 경보 액션(Top Alert 라벨) 추가
- 경보 액션 클릭 시 심층연구소 `Unknown Team Normalization Queue`로 자동 스크롤 + 하이라이트 포커스 연동
- 포커스 진입 시 첫 번째 PENDING 요청 카드도 자동 스크롤 + 에머랄드 링으로 즉시 조치 인지성 강화
- 통합대시보드 상단 부제목 모바일 정렬/줄바꿈 보강 및 한글 문구 전환
- 심층연구소 주요 영문 UI 라벨 한글화 + 히트맵/카드 텍스트 오버플로우 완화 클래스 적용
- 심층연구소 잔여 영문 라벨(상태/우선순위/경보/비교지표/지연사유) 추가 한글 통일 완료
- 통합대시보드/문서보관소 잔여 영문 라벨(`CRITICAL/WARNING`, `Last 7 Days`, `Top`, `Document Archive`, `Unknown`) 한글 통일
- TBM등록/위험성평가관리/App 복구 오버레이 잔여 영문 라벨(`AI Analysis`, `Processing`, `Base/Updates/New Regular`, `Unknown`, `System Restoring`) 한글 통일
- README를 한국어 사용자 설명서 형태로 확장(운영 시나리오/역할별 가이드/백업·복구 절차)
- 보관소 현재 목록 기준 `미연계 / 미일치 / 동일월 연계` 요약 배너 추가
- 심층연구소 월별 연계 추이 차트에 목표선(`90%`) 추가
- 대시보드 선택 팀 기준 `보정 바로가기`도 해당 팀 이슈 TBM 우선 연결
- 보관소 요약 배너에 `보정 필요만 켜기/해제` 원클릭 액션 추가
- 심층연구소 목표선을 운영값(localStorage)으로 관리하고 프리셋/직접입력 지원
- 연계율 목표값을 Settings 전역 설정으로 승격하고 DataLab 목표선과 동기화
- 보관소 요약 배너에 `미연계만 / 미일치만` 원클릭 세부 필터 추가
- 대시보드에서 선택한 이슈 팀 컨텍스트를 보관소 이동 시 전달하고 자동 필터 적용
- 대시보드 경보 카드에서 `미연계/미일치` 세부 필터를 보관소 초기 상태로 함께 전달
- 통합대시보드 모바일 최상단 헤더 정렬/여백 보정으로 상단 배치 개선
- 타워크레인 풍속 경보 기준을 `15m/s 이상`으로 상향
- 보관소 상단에 초기 필터 상태 배지(예: `대시보드 전달: 미연계`) 표시
- 통합대시보드 모바일 상단(헤더/배너/첫 카드) 간격을 1단계 추가 압축
- 통합대시보드 연계 경보 카드 액션 버튼을 모바일 그리드 + 최소 터치영역(44px)으로 개선
- 통합대시보드 팀별 보정 우선순위 카드 버튼도 동일 터치 기준(44px)으로 통일
- 히스토리/시스템 아이덴티티/TBM 등록 화면의 잔여 영문 라벨을 한글로 최종 통일(`Devlog`, `System Identity`, `Total Score`, `Leader's Action Card` 등)
- 대시보드/심층연구소 모바일 밀집 구간 라벨을 축약해 버튼·배지 가독성 보강(`보정 이동`, `보관소 전체 확인`, `검증로그 복사`, `통합검증 충족` 등)
- TBM 동영상 업로드 검증 강화 및 파일 크기 기반 자동 압축 프로파일 적용(`BALANCED/FAST/ULTRA_FAST`)으로 대용량 영상의 빠른 코칭 분석 체감 속도 개선
- TBM 동영상 업로드 직후 4가지 평가 항목(일지 작성/참석 참여도/작업자 집중도/팀장 리딩) 수동 채점 폼 활성화로 관리자 직접 채점 지원
- 동영상 재생 호버 오버레이를 통한 모바일 재생 UX 개선
- AI 분석을 보조 역할로 변경하여 관리자 판단 우선시
- TBM 동영상 분석 ETA를 파일 크기+압축 프로파일 보정형으로 정밀화해 초반 남은 시간 표시 흔들림 완화
- 위험성평가 업로드 동선을 `복구(JSON)` / `문서분석(PDF·이미지)`로 명확화하고 복구 입력에 JSON 형식 가드를 적용해 운영 혼선 방지
- TBM 모바일 동영상 업로드 실패 케이스 점검 후 MIME+확장자 동시 판별, 동일 파일 재선택 감지, `accept` 확장으로 업로드 안정성 보강

### 스마트TBM지휘 구현 순서 규칙
1. Command Phase 1 완료 전 Command Phase 2 착수 금지
2. 각 Command Phase는 `Clear 기준` 전체 충족 시 완료 처리
3. 완료 시 `CHANGELOG.md`와 트래커에 동시 반영

### 스마트TBM지휘 즉시 착수 항목 (다음 액션)
- [x] `types.ts`에 지시 스키마 초안 추가 (`CommandTask`) — 2026-04-29
- [x] `components/SafetyDataLab.tsx`에 지휘브리핑 섹션 스켈레톤 추가 — 2026-04-29
- [x] 트래커에 Command Phase 보드 생성 후 진행 상태 관리 시작 — 2026-04-29

---

## 구현 순서 규칙
1. Phase 1 완료 전 Phase 2 착수 금지
2. 각 Phase는 `Clear 기준` 전체 충족 시 완료 처리
3. 완료 시 `CHANGELOG.md`에 반영
4. 배포 단위는 `RELEASE_TEMPLATE.md` 사용

## 진행 상태 요약
- 현재 단계: **All Phases Cleared** 🎉
- 상태: `완료 유지 + 후속 검증 진행중 — 2026-04-30`
- 다음 작업: 모바일 E2E 검증(Phase A) + Command Phase3/4 런타임 검증
- 스마트TBM지휘: `Command Phase 1~2 Cleared, Phase 3~4 기능구현 완료 + 실검증 대기`
- 모바일 B1: `터치 타겟 44px 통일 + 채점 폼 번호/텍스트영역 개선 + 출력 보고서 평가 텍스트 추가 완료 (2026-04-30)`

## 2026-04-29 검증 메모
- 전일 기록(Phase 1~4, Export 안정화) 문서-코드 교차 확인 완료
- 핵심 구현 지점(`SafetyDataLab`, `ReportView`) 존재 확인
- 에디터 진단 기준 오류 0건 확인
- 터미널 빌드 재검증은 GitHub VFS 경로 미마운트로 로컬 셸에서 직접 실행 불가
- 위험성평가 화면 모바일 검토 결과, 평가목록/상세/편집 액션이 협소해 평가자·실무자 모두 동선 저하 확인
- 위험성평가 화면에 모바일 탭 전환(`평가목록/평가내용`, `실무도구/최종목록`)과 관점별 요약 카드 적용 완료
- 후속 보정으로 평가 선택 시 상세 자동 전환, 수동 추가 후 목록 복귀, `상위험만` 필터까지 반영 완료
- 추가 보정으로 모바일 하단 고정 액션바를 배치해 빠른 이동/상위험 전환/문서추가 동선까지 일원화 완료
- 실무자 조치기록 강화를 위해 `즉시조치 메모` 필드를 상위험 카드와 편집모드에 추가하고 백업 검증 스키마까지 반영 완료
- 평가자 검토 속도 향상을 위해 상위위험 `TOP 3` 상단 요약 카드와 `즉시조치 메모 복사` 기능까지 추가 완료
- 평가 목록 관리성을 위해 `기준 정보 / 운영 정보` 아코디언 축소와 `상위위험 고정 섹션`, `즉시조치 메모 일괄 복사`까지 반영 완료
- 스마트TBM 등록은 위험성평가 OCR 결과와 연계되어 있으며, 후속 보정으로 TBM 입력 일자와 동일한 월간 위험성평가를 우선 참조하도록 정밀화 완료
- 추가 보정으로 연계된 상위위험/즉시조치 메모를 스마트TBM 등록의 위험요인/대책 초안으로 단건·일괄 가져오기 가능하도록 실무 연계 완료
- 후속 확장으로 선택 팀 기준 우선 추천, 조치메모의 안전 코멘트 즉시 반영, TBM 저장 시 연계 위험성평가 메타데이터 기록까지 완료
- 저장 후 추적성 강화를 위해 대시보드 최근활동, 문서 보관소, 출력 보고서까지 연계된 위험성평가 라벨/동일월 여부가 보이도록 확장 완료
- 모바일 마감 보정으로 대시보드 `팀별 보정 우선순위`/`실시간 활동` 영역의 긴 팀명 overflow를 줄바꿈·폭 제한 기준으로 안정화 완료
- 보관소 상단 초기필터 배지에 `초기필터 해제` 원터치 액션을 추가해 대시보드 전달 컨텍스트를 즉시 기본값으로 복귀 가능하도록 반영 완료
- 심층연구소 팀 필터를 다중 선택(`teamIds`)으로 확장하고, 마스터에 없는 팀/미지정 팀까지 자동 포함해 평가 누락을 줄이는 운영형 필터 구조로 보강 완료
- 후속 운영 단계로 심층연구소에 `Unknown Team Normalization Queue`를 추가해 미등록 팀을 기존팀 치환 또는 신규팀 등록+치환으로 즉시 정규화 가능하도록 반영 완료
- 정규화 액션은 App 레벨에서 `entries/teams` 상태와 저장소를 동시에 갱신해 화면·분석·보관소 전 구간에 즉시 일관 반영되도록 구현 완료
- 정규화 액션마다 작업 이력(`작업자/시각/원본팀/대상팀/반영건수`)을 자동 저장하고 심층연구소에 최근 이력 카드로 가시화하도록 반영 완료
- 운영 연속성을 위해 `teamNormalizationLogs`를 전체 백업/복구 스키마에 포함해 재배포/복원 후에도 감사추적이 유지되도록 보강 완료
- 안정성 시뮬레이션 점검 후, 팀 정규화 로그 적재 경합 완화(최신 스냅샷 병합)와 실시간 날씨 stale 응답 방지(request sequence 가드)를 추가 반영 완료
- Phase A로 정규화 작업 이력 카드에 기간 필터(`오늘/7일/30일`)와 기간 기준 CSV 내보내기를 추가해 평가자/외부점검 대응 보고 동선을 단축하도록 반영 완료
- Phase B로 정규화 동선을 `요청 → 승인/반려` 워크플로우로 전환하고, 승인/반려 사유코드 및 검토 메모를 기록하는 거버넌스 체계를 반영 완료
- 워크플로우 상태(`teamNormalizationRequests`)를 저장/백업/복구에 연계해 세션/배포 변경 후에도 승인 대기·처리 이력이 유지되도록 보강 완료
- Phase C로 정규화 요청 SLA KPI(대기 건수/평균 대기시간/최장 대기시간/24시간 초과)와 반려 사유 Top 통계를 추가해 운영 병목과 반려 패턴을 즉시 파악 가능하도록 반영 완료
- 후속 자동 대응 단계로 KPI 임계치 기반 경보 배지(WARNING/CRITICAL)를 추가해 대기/반려 이상 징후를 보드 상단에서 즉시 인지 가능하도록 반영 완료

## 2026-04-29 다음작업 실행 절차 (출력물 실검증)
1. 프로젝트 루트 로컬 환경에서 `npm run dev` 실행
2. 보고서 센터에서 동일 샘플 1건 선택 후 `출력/PDF` 및 `이미지` 각각 생성
3. 아래 기준으로 단건 검증
	- A4 비율/외곽선 2px/행열 경계선 유지
	- 헤더·본문·푸터 높이 비율 붕괴 여부
	- 아이콘/텍스트 정렬, 줄바꿈 위치, 서명 셀 비율
4. 5건 이상 다건 출력 재검증
	- 페이지 순서와 이미지 파일 순서 일치
	- ZIP 파일 누락/중복 없음
5. 이상 발생 시 `components/ReportView.tsx` 우선 점검 후 `CHANGELOG.md`/트래커 작업 로그 갱신

### 오늘 종료 조건 (Done Definition)
- 단건/다건 검증 항목 전체 체크 완료
- 작업 로그 최종 1줄 반영(`Done` 또는 이슈 시 `In Progress`)
- 이슈 조치가 있었다면 `CHANGELOG.md`에 당일 변경 반영

## 종료 핸드오프 (내일 즉시 재개용)

### 현재 확정 상태
- SafetyDataLab v2 Phase 1~4 전부 Clear 완료
- 리포트 PDF/이미지 내보내기 균형감 보정 로직 반영 완료
- 최근 빌드 상태: `npm run build` 성공 (2026-04-28)

### 내일 시작 순서 (권장)
1. `npm install` (필요 시)
2. `npm run dev`
3. 보고서 센터에서 동일 샘플 1건으로 화면/PDF/이미지 비교 검증
4. 이상 없으면 다건(5건 이상) 일괄 내보내기 재검증

### 내일 점검 체크포인트 (출력 품질)
- 아이콘 위치, 텍스트 줄바꿈, 그리드 경계선 정렬
- 서명 셀 비율/정렬, 사진 셀 잘림 여부
- 페이지 여백/외곽선(2px) 유지 여부
- ZIP 이미지 파일명/순서 및 PDF 페이지 순서 일치

### 이슈 발생 시 우선 확인 파일
- `components/ReportView.tsx` (html2canvas/jsPDF 캡처 경로)
- `components/ReportCenter.tsx` (출력 진입/대상 선택)
- `CHANGELOG.md` (오늘 반영 내역)
